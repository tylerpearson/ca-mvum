# Implementation Plans

Generated by the improve skill on 2026-07-01 (audit at commit `ea0a597`,
standard depth, full repo). Plans 001–005 cover the top findings by leverage;
plans 006–009 were added the same day at the maintainer's selection (backlog
promotions + the dark-theme decision: **amend docs, don't build**). Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

Repo ground rules for every executor: `main` auto-deploys to production
(Cloudflare Pages) — **never commit to `main`; always branch + PR.**

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Trails in popups + fire intersection; escape popup HTML | P1 | S | — | DONE (PR #6) |
| 002  | Verification baseline: vitest + pytest + CI | P1 | M | — (001 recommended first) | DONE (PR #8) |
| 003  | Map 4WD/2WD >50″ classes into the suv4x4 profile | P1 | S | 002 (for tests) | DONE (PR #9) |
| 004  | Local-date default + non-leap day-of-year alignment | P2 | S | 002 | DONE (PR #10) |
| 005  | Per-class season windows in tiles + per-token evaluation | P2 | L | 002, 004 (003 recommended) | DONE (PR #13; tiles rebuilt + shipped 2026-07-02) |
| 006  | Hygiene sweep: stale comments, dead code, social meta, AQI wording, snow status | P2 | S | — (after 001/004 to avoid main.ts conflicts) | DONE (PR #12) |
| 007  | Repo CLAUDE.md: deploy rules, commands, couplings | P2 | S | — (best after 002/004/006 land, so facts are final) | DONE (PR #14) |
| 008  | Automated data refresh: vintage stamping + scheduled PR workflow | P2 | M | 002 | DONE (PR #11; see proof-run finding below) |
| 009  | Amend DESIGN.md/PRODUCT.md to the light-only theme decision | P3 | S | — | DONE (PR #7) |

All nine plans executed 2026-07-01/02 by dispatched executor agents (advisor-
reviewed diffs, CI-gated squash merges).

### Proof-run finding (2026-07-02): CI fetch gets empty data from USFS

The first `workflow_dispatch` of `data-refresh.yml` FAILED, and usefully so:
the USFS EDW service returned **0 features for all 17 forests** on the GitHub
runner (HTTP 200s with empty feature arrays — the same queries return data
from a residential IP, so the runner/datacenter IPs are evidently filtered
upstream). tippecanoe's "did not read any valid geometries" error (exit 110)
failed `make data` before the PR step, so no empty-tile PR was opened — but
that guard is accidental. **Backlog hardening**: `fetch_mvum.py` should exit
non-zero when the statewide total is 0 (or any forest is EMPTY) so the
workflow fails on data quality, not by luck; and the scheduled refresh likely
needs a non-datacenter egress (self-hosted runner or local `make data`) to
work at all. Until then, treat `data-refresh.yml` as manual-with-supervision
and refresh locally.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **002 before 003/004/005/008**: they all add or update tests that 002's
  infrastructure (vitest + `@maplibre/maplibre-gl-style-spec` evaluator,
  pytest) provides. 002 also pins characterization tests that 004 and 005
  deliberately flip (each marked in the test files).
- **004 before 005**: both rewrite `web/src/legal.ts`; 004 fixes `dayOfYear`
  to the pipeline's non-leap convention, which 005's per-token window math
  assumes.
- **001 is independent** and the quickest safety win; do it first.
- **006 after 001/004** (soft): all three edit `web/src/main.ts` in different
  places; ordering just avoids rebase noise.
- **007 last-ish** (soft): CLAUDE.md documents commands and couplings that
  002 (make test), 004 (MONTH_START mirror), and 006 (snow status) create;
  writing it after them means fewer "(planned)" hedges. Its Step 2 handles
  either order.
- **008 and 006 both touch `web/index.html`** in adjacent regions (006: meta
  tags + snow status; 008: the data-vintage comment line). Either order works;
  expect a trivial rebase if run concurrently.
- **005 and 008 end with operator handoffs**: tile rebuild / workflow-dispatch
  verification need the maintainer (gitignored raw data, tippecanoe, GitHub
  environment) and must not be attempted by executors.

## Key audit evidence (for whoever picks this up later)

- 735 of 46,662 routes permit ONLY `4wd_gt50`/`2wd_gt50` classes → invisible
  as "open" to every profile (plan 003). Measured against
  `data/ca-statewide.geojson` on 2026-07-01.
- 2,885 routes (6.2%) have per-class season windows that genuinely disagree;
  the single representative window misfilters them (plan 005).
- Open trails (`routes-trail` layer) get no popup and are excluded from the
  fire-perimeter intersection (plan 001): `web/src/main.ts:225`,
  `web/src/fire.ts:84-86`.
- Date picker defaults to the UTC date; frontend day-of-year counts Feb 29
  while the pipeline never does (plan 004): `web/src/main.ts:113`,
  `web/src/legal.ts:13-17` vs `pipeline/normalize.py:74-80`.
- No tests existed anywhere; `tsc --noEmit` was the only gate (plan 002).
- `web/src/styles.css` contains zero dark-theme code despite DESIGN.md/
  PRODUCT.md describing one (plan 009; decision 2026-07-01: amend docs).

## Direction options (maintainer's call — grounded, not planned)

- **Keyed geocoder**: `search.ts:8-12` documents Nominatim's public endpoint
  as a personal-use stopgap and names the swap (Mapbox/MapTiler key or
  self-hosted) as a known follow-up if traffic grows.

(Resolved since the audit: data-refresh automation → plan 008; dark theme →
plan 009, decided as docs amendment; og:image → folded into plan 006;
CLAUDE.md → plan 007.)

## Findings considered and rejected

- **Nominatim public endpoint usage**: by design for a personal project;
  policy constraints are documented in `search.ts` and honored (search on
  submit only). Revisit only via the direction item above.
- **`glyphs: demotiles.maplibre.org` in `main.ts:41`**: demo server, but no
  symbol layers exist so glyphs are never fetched; harmless. Deliberately
  left in place by plan 006 (removing it would silently break future label
  layers).
- **`npm audit`**: 0 vulnerabilities (checked 2026-07-01). Dependencies are
  current majors (Vite 6, MapLibre 5, TS 5.7); no migration findings.
- **Tile-deploy workflow security**: already hardened (SHA-pinned actions,
  least-privilege token, main-only guard). No finding.
- **Fire/smoke/AQI fetch error handling**: each toggle has a friendly failure
  state and `console.error`; adequate for the surface.
- **`recomputeAffected` O(routes×fires) turf loop**: debounced on `moveend`,
  viewport-scoped; fine at realistic feature counts. Not worth optimizing.
